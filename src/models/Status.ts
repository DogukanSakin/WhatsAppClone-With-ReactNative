export default class {
  id: number;
  contactID: number;
  time: string;
  constructor(id: number, contactID: number, time: string) {
    this.id = id;
    this.contactID = contactID;
    this.time = time;
  }
}
